#include "script_component.hpp"

//if (!isServer) exitWith {};
_this remoteExecCall ["grad_vulcano_fnc_playerInit", 0, true];
systemChat "Starting Vulcano";

GVAR(smokeEffectTimer) = true;

[
    {
        params ["_vulcanoPos", "_radius", "_delay"];
        private _parameters = [(2 + random 300), (2 + random 300), (5 + random 20), (1 + random 10), (20 + random 10), (100 + random 100), (200 + random 200)];
        [QGVAR(smokeEffect), [_vulcanoPos, 2, _radius, _delay, _parameters]] call CBA_fnc_globalEvent;
    },
    _this,
    5
] call CBA_fnc_waitAndExecute
